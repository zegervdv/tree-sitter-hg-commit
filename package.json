{
  "name": "tree-sitter-hg-commit",
  "version": "1.0.0",
  "description": "hg-commit tree-sitter grammar",
  "main": "bindings/node",
  "scripts": {
    "test": "tree-sitter test",
    "format": "prettier --write grammar.js package.json",
    "format-check": "prettier --check grammar.js package.json"
  },
  "author": "zegervdv",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.18.0"
  },
  "devDependencies": {
    "prettier": "^3.1.1",
    "tree-sitter-cli": "^0.20.8"
  }
}
